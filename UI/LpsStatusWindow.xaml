<Window
    x:Class="Renumber.UI.LpsStatusWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="LPS Status"
    Width="240"
    SizeToContent="Height"
    MinWidth="180"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStyle="None"
    Topmost="True"
    ShowInTaskbar="False"
    WindowStartupLocation="Manual"
    MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border
        Background="#FF222222"
        BorderBrush="#FF555555"
        BorderThickness="1.5"
        CornerRadius="4"
        Padding="12,10,12,12">
        <StackPanel>

            <!-- Header row: title + pick count -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="LPS"
                           Foreground="#FFFFFFFF"
                           FontSize="11"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="picks: "
                               Foreground="#FFFFFFFF"
                               FontSize="11" />
                    <TextBlock x:Name="PickCountText"
                               Text="0"
                               Foreground="#FFFFFFFF"
                               FontSize="11"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Grid>

            <!-- Separator -->
            <Rectangle Height="1" Fill="#FF444444" Margin="0,0,0,8" />

            <!-- Parameter rows -->
            <ItemsControl x:Name="ParamRowsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,3,0,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Name}"
                                       Foreground="#FFFFFFFF"
                                       FontSize="12"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Value}"
                                       Foreground="#FFFFFFFF"
                                       FontSize="13"
                                       FontWeight="Bold"
                                       Margin="10,0,0,0"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Nudge buttons + Alt hint -->
            <Rectangle Height="1" Fill="#FF383838" Margin="0,8,0,6" />
            <Grid Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        x:Name="NudgeUpButton"
                        Content="▲"
                        Click="NudgeUp_Click"
                        Height="26" FontSize="13"
                        Background="#FF333333" Foreground="White"
                        BorderBrush="#FF555555" BorderThickness="1"
                        Cursor="Hand" />
                <Button Grid.Column="2"
                        x:Name="NudgeDownButton"
                        Content="▼"
                        Click="NudgeDown_Click"
                        Height="26" FontSize="13"
                        Background="#FF333333" Foreground="White"
                        BorderBrush="#FF555555" BorderThickness="1"
                        Cursor="Hand" />
            </Grid>
            <TextBlock Text="Alt = hold  •  ▲/▼ = adjust value"
                       Foreground="#FFFFFFFF"
                       FontSize="10"
                       HorizontalAlignment="Center" />

        </StackPanel>
    </Border>
</Window>
